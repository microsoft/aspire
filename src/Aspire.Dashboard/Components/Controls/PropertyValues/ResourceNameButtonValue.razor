@using Aspire.Dashboard.Utils

@if (_resourceIconName is { } iconName)
{
    @* Only display a button if dashboard client is enabled and a matching resource can be found. *@
    <FluentButton OnClick="OnClickAsync" Appearance="Appearance.Lightweight" Class="button-hyperlink">
        <DeckIcon Name="iconName" Size="16" Class="resource-icon" Style="margin-inline-end: 6px;" />
        <FluentHighlighter HighlightedText="@HighlightText" Text="@Value" />
    </FluentButton>
}
else
{
    <FluentHighlighter HighlightedText="@HighlightText" Text="@Value" />
}
