BREAK capability-removed Aspire.Hosting Aspire.Hosting.Pipelines/PipelineStep.dependsOnSteps -- https://github.com/microsoft/aspire/pull/17286 -- Pipeline step dependencies are configured through dependsOn(...) and addStep/withPipelineStepFactory options instead of exposing the init-only backing collection.
BREAK capability-removed Aspire.Hosting Aspire.Hosting.Pipelines/PipelineStep.requiredBySteps -- https://github.com/microsoft/aspire/pull/17286 -- Pipeline step reverse dependencies are configured through requiredBy(...) and addStep/withPipelineStepFactory options instead of exposing the init-only backing collection.
BREAK capability-removed Aspire.Hosting Aspire.Hosting.Pipelines/PipelineStep.tags -- https://github.com/microsoft/aspire/pull/17286 -- Pipeline step tags are configured through addTag(...) and pipeline step factory options, and selected through stepsByTag(...), instead of exposing the init-only backing collection.
BREAK handle-flag-removed Aspire.Hosting Aspire.Hosting/Aspire.Hosting.Pipelines.PipelineStep.ExposeProperties -- https://github.com/microsoft/aspire/pull/17286 -- PipelineStep uses explicit ATS-friendly name/description projections and mutation methods instead of broad property exposure for init-only properties.
