# Changelog

This package `__PACKAGE_NAME__` bundles the Aspire CLI at version `__VERSION__`.

Aspire publishes detailed release notes for every release on GitHub. Each release lists new features, fixes, and any breaking changes so you can see what changed before updating.

- Aspire release notes: <https://github.com/microsoft/aspire/releases>

Find the entry that matches version `__VERSION__` on the releases page. Some stable, prerelease, and daily packages use release tag names that do not map directly to the npm package version.
