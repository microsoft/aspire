{
  "name": "foundry-agent-toolbox-ts",
  "version": "1.0.0",
  "description": "TypeScript AppHost wiring up a Foundry Agent + Toolbox + custom auth-protected MCP server.",
  "type": "module",
  "private": true,
  "scripts": {
    "start": "aspire run",
    "build": "tsc --noEmit",
    "dev": "tsc --noEmit --watch"
  },
  "dependencies": {
    "vscode-jsonrpc": "^8.2.0"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "tsx": "^4.19.0",
    "typescript": "^5.3.0"
  }
}
